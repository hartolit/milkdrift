[package]
name = "sampling"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic zero-allocation logit sampling and stop matching"
readme = "README.md"
publish = false

[package.metadata.milkdrift]
role = "domain-feature"
responsibility = "Perform deterministic allocation-free logit sampling and stop matching"
benchmark-targets = ["sampling_pipeline"]

[dependencies]
domain-contracts.workspace = true
libm.workspace = true

[dev-dependencies]
criterion.workspace = true
stats_alloc.workspace = true

[[test]]
name = "allocation"
path = "tests/allocation.rs"
harness = false

[[bench]]
name = "sampling_pipeline"
harness = false

[lints]
workspace = true
