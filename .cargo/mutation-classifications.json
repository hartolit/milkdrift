{
  "schema_version": 1,
  "cargo_mutants_version": "27.1.0",
  "policy": {
    "survivors_require_exact_mutant_identity": true,
    "allowed_classifications": [
      "equivalent",
      "unreachable_by_valid_contract",
      "tool_limitation"
    ],
    "unclassified_survivors_fail_the_lane": true
  },
  "classifications": [
    {
      "mutant": "adapters/redb-store/src/application.rs:103:29: replace || with && in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "An equal validated layout digest is content-bound to both generation and document, so exactly one of those facts cannot differ without persisted corruption or a digest collision."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:102:49: replace != with == in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "The branch is a corruption assertion after equal layout digests; validated layout updates cannot retain the digest while changing only generation."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:103:51: replace != with == in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "The branch is a corruption assertion after equal layout digests; validated layout updates cannot retain the digest while changing only document content."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:158:25: replace || with && in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "The proposal table key is canonically derived from run, proposal, and proposed revision; a decoded row differing in one keyed fact requires out-of-band database corruption."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:157:25: replace || with && in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "The proposal table key is canonically derived from run, proposal, and proposed revision; a decoded row differing in one keyed fact requires out-of-band database corruption."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:156:37: replace != with == in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "A proposal row is inserted only beneath its canonical run/proposal/revision key, so a run mismatch can arise only from physical corruption, which this defensive assertion reports."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:157:46: replace != with == in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "A proposal row is inserted only beneath its canonical run/proposal/revision key, so a proposal mismatch can arise only from physical corruption, which this defensive assertion reports."
    },
    {
      "mutant": "adapters/redb-store/src/application.rs:158:55: replace != with == in <impl ApplicationCommandStore for RedbStore>::commit_application_command",
      "classification": "unreachable_by_valid_contract",
      "explanation": "A proposal row is inserted only beneath its canonical run/proposal/revision key, so a revision mismatch can arise only from physical corruption, which this defensive assertion reports."
    },
    {
      "mutant": "crates/runtime/src/engine/reconciliation.rs:28:13: replace || with && in RuntimeService::plan_revision_adoption",
      "classification": "unreachable_by_valid_contract",
      "explanation": "A non-active projection without a termination record cannot produce a valid reconciliation history or plan; the ordinary command path rejects that invalid state after this defensive early guard as well."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:192:17: replace || with && in <impl PeerExecutionStore for RedbStore>::admit_peer_execution",
      "classification": "unreachable_by_valid_contract",
      "explanation": "The request index and stored snapshot are written atomically from the same validated owner/request identity; a one-field mismatch requires out-of-band database corruption."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1939:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1928:5: replace validate_record -> Result<(), PersistenceError> with Ok(())",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This removes only decode-time corruption validation for a canonical record that validated transactional APIs cannot construct with invalid primary facts."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1938:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1937:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1936:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1935:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1934:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1933:9: replace || with && in validate_record",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a canonical execution record produced only by validated transactional transitions; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1981:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1966:5: replace validate_tombstone -> Result<(), PersistenceError> with Ok(())",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This removes only decode-time corruption validation for a tombstone that is atomically derived from a validated terminal or uncertain record."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1980:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1979:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1978:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1977:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1976:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1975:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1974:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1973:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1972:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1971:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1970:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1969:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1968:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1967:9: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1995:17: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1994:17: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1993:17: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1995:52: replace > with == in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1995:52: replace > with >= in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2009:17: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2008:17: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2007:17: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2007:42: replace > with == in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2007:42: replace > with >= in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2009:33: replace > with == in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2009:33: replace > with >= in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2017:13: replace || with && in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:2020:74: replace != with == in validate_tombstone",
      "classification": "unreachable_by_valid_contract",
      "explanation": "This is a decode-time corruption sentinel over a tombstone atomically derived from a validated terminal or uncertain record; the invalid single-field state is unreachable through the public persistence contract."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1026:25: replace match guard matches!( record.phase, PeerExecutionPhase::CancellationRequested { .. } ) with true in <impl PeerExecutionStore for RedbStore>::recover_peer_claims",
      "classification": "equivalent",
      "explanation": "The preceding match arm already consumes every claimed dispatch record with no entry evidence, so this later arm is reached only for a claimed cancellation record and the guard is necessarily true."
    },
    {
      "mutant": "adapters/redb-store/src/peer.rs:1066:13: replace > with < in <impl PeerExecutionStore for RedbStore>::recover_peer_claims",
      "classification": "equivalent",
      "explanation": "The redb table length is unsigned, so replacing length greater than zero with length less than zero is always false; exact empty and nonempty recovery-frontier behavior is covered independently."
    }
  ]
}
