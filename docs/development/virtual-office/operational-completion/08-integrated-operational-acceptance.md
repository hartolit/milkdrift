# Assignment 08 — Accept the integrated product and correct evidence, not architecture

## Outcome

Independently verify the operational results of Assignments 01–07 and correct substantiated defects
in their integration. Measure the review's remaining query-cost concern and refresh the current
evidence summary. Do not start another repository-wide cleanup, rebuild evidence tooling, or begin
Iced. If the implementation is sound, acceptance need not manufacture code changes.

Read the sprint README, `AGENTS.md`, canonical product/architecture docs, workflow, implementation
and documentation practices, and current handoffs. Review the actual diff and source; do not assume
a prior agent's summary or a `Success` terminal establishes completion.

## 1. One integrated product proof

Use actual daemon/client binaries and existing controlled process/model fixtures, not a substitute
runtime assembled only in tests. Exercise:

```text
fresh task -> independent verification -> unusable review or failure
   -> gate holds dependent work
   -> installed controller selects causal evidence and proposes remediation
   -> required authorization/approval
   -> prospective revision, preserving the original failed history
   -> remediation -> accepted result -> stop or bounded continuation
```

Prove current account identity and remaining allowance through the public inspector. Exhaust a
cumulative limit after more than one invocation; no fresh run/revision/retry may reset the account.
Contrast a per-request grant limit with a cumulative account so output cannot suggest they are the
same. Test unknown metering, exact command replay, revocation at preparation/entry, and restart at
reservation, acceptance, approval and reconciliation boundaries. Reuse earlier tests where they
already prove these facts on unchanged source; add only missing integrated coverage.

Exercise ExplicitContinuation and Fresh side by side, exact peer placement to an approved host,
reconnect without duplicate remote entry, read-only inspection of a blocked generation, verified
backup/restore without automatic external execution, and inherited-pipe cleanup during shutdown.
Do not weaken historical manifests, authority, identity, unknown effects, or artifact checks to make
this chain complete.

Review controller activation separately from build success. Reconcile the finite prerequisite list
from Assignment 03 with current code/tests and external evidence. If required external qualification
remains unavailable, the correct result is a clearly blocked activation claim with a specific case,
not fictional readiness and not an endless new validation project. Other accepted product improvements
can be reported precisely without calling the full controller vision delivered.

## 2. Measure historical queries before changing storage

The source review established bounded memory but possible long historical scans, not a measured
latency regression. Start from current authorized CLI/API attempt-inspector and causal-context
operations and the actual workload just exercised. Use the existing evidence/benchmark harness or a
small ignored `target/` reproduction; do not add a new benchmark framework.

Measure at a few controlled history sizes with fixed live obligations. Record rows/pages decoded,
bytes read, peak retained collections, and representative elapsed timings with environment/source
identity. Separate known-execution lookup, broad history browsing, and context selection. Include
near-start and near-end targets and protected-resource filtering. Do not assert universal timing
limits from one machine or call ordinary cold-history growth a memory leak.

If a concrete operator wait or excessive repeated scan is demonstrated, fix the narrow owning query.
Compare using an existing cursor/summary with adding one justified rebuildable index; retain primary
journal authority, crash consistency, idempotency, version policy and authority filtering. Test
missing/stale index behavior, restart, pagination and full-history equivalence. Do not introduce an
index per event family or a new cache that duplicates truth. If measured costs are acceptable, retain
the current code and document the workload and trigger for reconsideration. That is a valid outcome.

## 3. Check marginal complexity and evidence tooling

Review the changed ownership paths, especially `crates/control/src/service.rs`,
`adapters/model-provider/src/adapter.rs`, `crates/prompt-sequence/src/compiler.rs`, and evidence
extensions. The review did not establish that these must be split. Refactor only mixed responsibilities,
duplicated mechanisms, dead superseded paths, or change amplification demonstrated by this sprint.
Use private modules and existing shared owners before adding crates or traits. No line-count target,
public-API removal campaign, or rewrite of coherent contract variants.

Existing evidence tooling is substantial. Each added fixture/report/check must prove a named invariant
or interoperability claim not already covered. Consolidate duplicate setup/report logic introduced
by the sprint; do not weaken the gate or remove independent failure cases to reduce test count. Keep
raw logs under `target/`/CI. Do not copy production formulas into test oracles, hardcode arbitrary
expected-byte totals, or generate giant tracked transcripts. Preserve one owner per schema/version.

## 4. Current evidence and documentation truth

Update `docs/product/status.md`, affected operations/guides, `docs/development/verification-evidence.md`,
roadmap, API/adapter/session/placement matrices and examples to match accepted behavior. Do not
reconstruct `docs/product/vision.md` or duplicate chronology.

The input review recorded successful quality run `34595978663` and platform run `34595978543` at
`aa77c483626b100e0f550c63648875ba28182238`. Treat those as review-recorded evidence, not tests you just
ran. Correct the old pending macOS note using exact confirmed scope. A selected contract/process
suite on Windows/macOS/Linux is not a complete system qualification on every OS. Record new checks
against their own commit and environment; don't relabel old evidence as new.

The accepted external Codex/LM Studio evidence covers its recorded Windows configuration and settled
restart boundaries. It did not verify thinking mode, arbitrary model quality, peer operation,
controller activation, or filesystem power loss. Incorporate newly executed evidence only within its
actual scope. Requested generation settings, operator declarations, observed effective settings,
and unknowns remain distinct. Evaluate the two existing generation/provenance whiteboard topics
only where this sprint contributed evidence; do not mark the broader questions resolved by proximity.

Keep explicit remaining limits: unsupported provider-managed/process sessions, trusted-host rather
than sandboxed execution, no universal migration or destructive online cleanup, no peer discovery,
and no UI. If any limit changed, cite the exact implementation and tests that now support the claim.

## 5. Verification and closure

Use the full gate in `docs/development/workflow.md` for the integrated executable diff, plus current
repository documentation/contracts, actual-binary operator/model scenarios, affected two-daemon,
account/recovery, and process-platform suites. Inspect actual test discovery and CI job scope. Target
mutation/fault evidence at changed authority/account/outcome/entry/placement boundaries rather than
requiring unrelated whole-project campaigns. One owner coordinates Cargo jobs and shared files.

A real external follow-through uses only explicitly authorized test repositories/endpoints and
bounded allowances. Do not edit live virtual-office services, weaken byte pinning, or spend credits
because a profile exists. Missing evidence remains a precise blocker to its claim.

Report accepted results by assignment, corrected integration findings, exact commands and environment,
measured query outcome, schema/upgrade consequences, remaining supported limitations and controller
activation decision. Distinguish a complete implementation from demonstrated operating qualification.
Do not claim “production-ready” from a green build alone.

Stop when each assigned acceptance criterion is implemented/proven or has an explicit coordinator-
accepted disposition, the combined workflow is demonstrated, and current documentation is truthful.
No arbitrary progress percentage or new sprint. Follow the virtual-office procedure: move lasting
facts to canonical owners, retain unresolved broader topics on the whiteboard, remove the completed
sprint and its office index entry only after accepted coverage, and verify links. The coordinator may
retain this sprint while any assignment or essential qualification remains genuinely blocked.
