[package]
name = "application-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []
cuda = ["candle-backend/cuda"]

[dependencies]
candle-backend = { path = "../../adapters/candle-backend" }
