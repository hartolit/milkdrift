[package]
name = "desktop-slint"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.milkdrift]
role = "application"
responsibility = "Run the fixture CUDA application"

[features]
default = []
cuda = ["application-runtime/cuda"]

[dependencies]
application-runtime = { path = "../../runtime/application-runtime" }
