[package]
name = "adapter"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.milkdrift]
role = "adapter"
responsibility = "Adapt the fixture platform"

[dependencies]
platform = { path = "../../platform/platform" }
