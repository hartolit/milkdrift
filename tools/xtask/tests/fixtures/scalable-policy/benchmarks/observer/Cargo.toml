[package]
name = "observer"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.milkdrift]
role = "benchmark-observer"
responsibility = "Observe fixture application behavior"
benchmark-targets = ["runtime"]

[dependencies]
app = { path = "../../crates/apps/app" }

[[bench]]
name = "runtime"
harness = false
